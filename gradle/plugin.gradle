gradlePlugin {
  plugins {
    checkerFrameworkPlugin {
      id = "com.jaredsburrows.checkerframework"
      implementationClass = "com.jaredsburrows.checkerframework.CheckerFrameworkPlugin"
    }
  }
}
